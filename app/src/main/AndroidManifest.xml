<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.b21_cap0148.m_posyandu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MPosyandu">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".EditAnakActivity" />
        <activity android:name=".DetailAnakActivity" />
        <activity android:name=".DataAnakActivity" />
        <activity android:name=".HasilPengukuranActivity" />
        <activity android:name=".InputPengukuranActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>
</manifest>